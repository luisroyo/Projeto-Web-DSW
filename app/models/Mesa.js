class Mesa {
  constructor(id, mesaNumber, capacidade, disponibilidade) {
    this.id = id;
    this.mesaNumber = mesaNumber;
    this.capacidade = capacidade;
    this.disponibilidade = disponibilidade;
  }
}

module.exports = Mesa;
